export const MyMacrosConstants = {
  /**
  * @returns {RegExp} /^(\d*\.)?\d+$/
  */
  REGEX_DECIMAL_PATTERN : /^(\d*\.)?\d+$/,

  /**
  * @returns {RegExp} /^[0-9]+$/
  */
  REGEX_INTEGER_PATTERN : /^[0-9]+$/
};
